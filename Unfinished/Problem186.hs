module Euler.Problem186
( problem186
) where

-- import Data.Function.Memoize

-- s = memoFix laggedFibonacci
    -- where laggedFibonacci :: (Integer -> Integer) -> Integer -> Integer
          -- laggedFibonacci f k
              -- | k <= 55   = (100003 - 200003*k + 300007*k^3) `mod` 1000000
              -- | otherwise = (f (k-24) + f (k-55)) `mod` 1000000


